{
  "enabledPlugins": {
    "discover@claude-code-discover": true
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file_path=$(echo \"$TOOL_INPUT\" | jq -r '.file_path // empty'); if [ -n \"$file_path\" ] && echo \"$file_path\" | grep -qE '\\.rs$' && echo \"$file_path\" | grep -q 'rustSDK/'; then cd rustSDK && cargo fmt --quiet -- \"$file_path\" 2>/dev/null; fi",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
